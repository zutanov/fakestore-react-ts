import styled from 'styled-components';

export const SDescription = styled.div``;

export const SDescriptionText = styled.p``;

export const SDescriptionBtn = styled.button`
    margin-top: 10px;
    font-weight: bold;
    color: darkblue;
    border: none;
    background: none;
    cursor: pointer;
`;
