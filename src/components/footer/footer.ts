import styled from 'styled-components';
import { SHeader, SHeaderWrapper } from '../header/header';

export const SFooter = styled(SHeader)``;
export const SFooterWrapper = styled(SHeaderWrapper)``;
export const SFooterDesc = styled.h4`
    font-size: 18px;
`;
